version: 2

models:
  - name: dim_date
    description: "Uma linha por dia, do primeiro ao ultimo dia observado em stg_trips."
    columns:
      - name: date_key
        tests:
          - unique
          - not_null
      - name: date_day
        tests:
          - unique
          - not_null
